# CLAUDE.md

> Entry point for Claude Code.
>
> **[AGENTS.md](AGENTS.md) is authoritative** — read it before touching `src/`. Its layer
> boundaries are ESLint-enforced, so violating them fails the build.

Quickstart: [README.md](README.md). Run `nvm use && npm install && npm test`.

Deliberately a pointer, not a summary — a second copy of the rules would drift from the first.
